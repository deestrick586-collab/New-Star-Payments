<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>Smart Orb – Animated Glow</title>
<style>
  :root{
    --bg:#0b0f14;
    --orb-core:#7ef9ff;
    --orb-mid:#7a5cff;
    --orb-outer:#00ffc6;
    --wm:#ffffff10; /* watermark */
  }
  @media (prefers-color-scheme: light){
    :root{ --bg:#0a0a0a; }
  }
  *{ box-sizing:border-box }
  html,body{ height:100% }
  body{
    margin:0;
    display:grid;
    place-items:center;
    background:radial-gradient(1200px 900px at 70% 20%, #141a22 0%, var(--bg) 60%);
    color:#cdd7e1;
    font-family:system-ui,-apple-system,Segoe UI,Roboto,Inter,Arial,sans-serif;
    overflow:hidden;
  }

  /* Canvas wrapper */
  .stage{
    position:relative;
    width:min(80vmin,640px);
    aspect-ratio:1/1;
    display:grid;
    place-items:center;
    filter: drop-shadow(0 10px 30px #0008);
  }

  /* The orb */
  .orb{
    position:relative;
    width:100%;
    height:100%;
    border-radius:50%;
    background:
      radial-gradient(40% 40% at 50% 35%, #ffffff, var(--orb-core) 35%, transparent 45%),
      radial-gradient(70% 70% at 60% 60%, var(--orb-mid), transparent 60%),
      radial-gradient(100% 100% at 40% 70%, var(--orb-outer), transparent 70%),
      radial-gradient(65% 65% at 30% 30%, #00ffff30, transparent 60%);
    overflow:visible;
    animation:
      breathe 6s ease-in-out infinite,
      hue 18s linear infinite;
    isolation:isolate; /* keep glow layers clean */
  }

  /* Soft animated aura ring */
  .orb::before,
  .orb::after{
    content:"";
    position:absolute;
    inset:-10%;
    border-radius:50%;
    filter: blur(30px);
    z-index:-1;
  }
  .orb::before{
    background:radial-gradient(closest-side, #7ef9ff55, transparent 70%);
    animation:pulse 4s ease-in-out infinite;
  }
  .orb::after{
    background:conic-gradient(from 0deg, #00ffc620, #7a5cff20, #7ef9ff20, #00ffc620);
    animation:spin 24s linear infinite;
  }

  /* Glass highlight sweep */
  .sheen{
    position:absolute;
    inset:0;
    border-radius:50%;
    -webkit-mask: radial-gradient(55% 55% at 35% 25%, #000 30%, transparent 60%);
            mask: radial-gradient(55% 55% at 35% 25%, #000 30%, transparent 60%);
    background:linear-gradient(120deg, #fff6, #fff0 30%);
    mix-blend-mode:screen;
    animation:sheen 7s ease-in-out infinite;
    pointer-events:none;
  }

  /* Center watermark */
  .watermark{
    position:absolute;
    inset:0;
    display:grid;
    place-items:center;
    pointer-events:none;
    user-select:none;
    font-weight:700;
    letter-spacing:0.12em;
    text-transform:uppercase;
    color:var(--wm);
    font-size: clamp(18px, 3vmin, 28px);
    backdrop-filter: blur(0.2px); /* super subtle anti-aliasing */
  }

  /* Animations */
  @keyframes breathe{
    0%,100%{ transform:scale(0.98) translateY(1px) }
    50%{ transform:scale(1.02) translateY(-1px) }
  }
  @keyframes pulse{
    0%,100%{ opacity:.55; transform:scale(.98) }
    50%{ opacity:.9; transform:scale(1.05) }
  }
  @keyframes spin{ to{ transform:rotate(360deg) } }
  @keyframes sheen{
    0%{ transform:rotate(-8deg) translateY(0) }
    50%{ transform:rotate(8deg) translateY(-1.5%) }
    100%{ transform:rotate(-8deg) translateY(0) }
  }
  @keyframes hue{
    0%{ filter:hue-rotate(0deg) }
    100%{ filter:hue-rotate(360deg) }
  }

  /* Respect reduced motion */
  @media (prefers-reduced-motion: reduce){
    .orb, .orb::before, .orb::after, .sheen{ animation:none }
  }

  /* Brand caption (optional) */
  .caption{
    position:absolute;
    bottom:-52px;
    width:100%;
    text-align:center;
    font-size:clamp(14px, 2.2vmin, 16px);
    color:#8ea8bf;
    opacity:.85;
    letter-spacing:.04em;
  }
</style>
</head>
<body>
  <div class="stage" aria-label="Smart Orb animated logo">
    <div class="orb" role="img" aria-label="Luminous orb with animated glow"></div>
    <div class="sheen"></div>
    <div class="watermark">Smart Orb™ • Orb Coins</div>
    <div class="caption">Smart Orb • Universal AI</div>
  </div>
</body>
</html>
